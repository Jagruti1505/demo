
const Util = require('../config/dataHandler.js');

describe('<enter scenario name>', () => {

    it('<Enter test case details>', async () => {

        //Please paste the recorded script over here
        const bodyoptions = { project_id: globalProject?.value };

getAllWebActions(bodyoptions).then((response) => {
  if (response) {
    console.log("response", response);
    console.log("applicationType?.value", applicationType?.value);

    const filteredWebAction = response?.filter((item) => {
      console.log("item", item);
      return item?.web_action_name === "Web"; // Add return statement here
    });

    console.log("filteredWebAction------", filteredWebAction);
    setWebActions(filteredWebAction);
  }
}).catch((error) => console.log(error));


    });
});
